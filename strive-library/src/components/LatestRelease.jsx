const LatestRelease = () => (
    <div>
        <container> 
            <row className="justify-content-center"> {
                categoryScifi.map((book) => {

                })
            }
            
            </row> 
        </container>
    </div>
)

export default LatestRelease